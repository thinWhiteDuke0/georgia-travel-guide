# CI/CD და განთავსება / CI and Deployment

## 1. უწყვეტი ინტეგრაცია (GitHub Actions)

ფაილი: `.github/workflows/ci.yml`

ეშვება ავტომატურად ყოველ `push`-სა და pull request-ზე `main` ბრანჩში.
საჯარო რეპოზიტორიისთვის სრულიად უფასოა.

პარალელურად სრულდება ხუთი დამოუკიდებელი სამუშაო:

| სამუშაო | რას ამოწმებს |
|---|---|
| **Backend · Go** | proto-ს გენერაცია, `go vet`, ფორმატირება, ტესტები, ექვსივე ბინარულის აგება |
| **Backend · .NET** | `dotnet restore` და `build` Release რეჟიმში |
| **Database** | ნამდვილ PostgreSQL 16-ზე ისხმება სქემა და seed, მოწმდება ჩანაწერების რაოდენობა |
| **Mobile · Flutter** | `flutter analyze`, ტესტები, web-ბილდის აგება |
| **Docker** | სამივე образის აგება |

შედეგი ჩანს რეპოზიტორიის **Actions** ჩანართში. მწვანე ნიშანი ნიშნავს,
რომ სისტემა სრულად იწყობა და ბაზა მუშაობს.

### ბეჯი README-სთვის

```markdown
![CI](https://github.com/thinWhiteDuke0/georgia-travel-guide/actions/workflows/ci.yml/badge.svg)
```

---

## 2. განთავსება Render-ზე (უფასო)

ფაილი: `render.yaml` (Render Blueprint)

### არქიტექტურული შენიშვნა

უფასო ტარიფი მკაცრად ზღუდავს ერთდროულად მომუშავე სერვისების რაოდენობას,
ამიტომ განთავსებისას **ოთხივე Go სერვისი ერთ კონტეინერში ეშვება**
(`backend/cmd/server/main.go`). კოდი უცვლელია: თითოეული სერვისი კვლავ
საკუთარ gRPC სერვერს უშვებს, ოღონდ loopback მისამართზე, ერთი პროცესის
შიგნით.

`docker-compose.yml` კვლავ ცალკე უშვებს ყველა სერვისს — სწორედ ის
კონფიგურაციაა აღწერილი ნაშრომში. განთავსების ვარიანტი მხოლოდ უფასო
ჰოსტინგის შეზღუდვას ემსახურება.

### ნაბიჯები

**1. Blueprint-ის შექმნა**

1. გადადით <https://dashboard.render.com> → **New** → **Blueprint**
2. დააკავშირეთ GitHub და აირჩიეთ `georgia-travel-guide`
3. Render თავად წაიკითხავს `render.yaml`-ს და შემოგთავაზებს 4 რესურსს

**2. საიდუმლო ცვლადების შევსება**

Render `sync: false` ველებს თავად არ ავსებს. მიუთითეთ:

| სერვისი | ცვლადი | მნიშვნელობა |
|---|---|---|
| guide-backend | `JWT_SECRET` | მინიმუმ 32 სიმბოლო, ნებისმიერი შემთხვევითი |
| guide-auth | `JWT_SECRET` | **ზუსტად იგივე**, რაც backend-ს |
| guide-backend | `AUTH_ADDR` | `guide-auth.onrender.com:443` |
| guide-web | `API_BASE_URL` | `https://guide-backend.onrender.com` |

> `JWT_SECRET` ორივე სერვისზე იდენტური უნდა იყოს: auth ხელს აწერს ტოკენს,
> gateway კი ამოწმებს. სხვაობის შემთხვევაში ავტორიზაცია ჩავარდება.

**3. თანმიმდევრობა**

`guide-auth` და `guide-backend` პირველ გაშვებაზე ერთმანეთის URL-ს ვერ
გაიგებენ. ამიტომ: ჯერ დაელოდეთ `guide-auth`-ის აწყობას, აიღეთ მისი მისამართი,
ჩაწერეთ `AUTH_ADDR`-ში და ხელახლა გაუშვით `guide-backend` (Manual Deploy).

**4. შემოწმება**

```
https://guide-backend.onrender.com/health        → {"status":"ok"}
https://guide-backend.onrender.com/api/cities    → ქალაქების სია
https://guide-web.onrender.com                   → აპლიკაცია
```

ბაზის სქემა და საწყისი მონაცემები **ავტომატურად** ისხმება პირველ გაშვებაზე
(`RUN_MIGRATIONS`, `RUN_SEED`). seed მხოლოდ ცარიელ ბაზას ავსებს, ამიტომ
გადატვირთვა მომხმარებლების მონაცემებს არ შლის.

---

## 3. მნიშვნელოვანი შეზღუდვები

| შეზღუდვა | გავლენა |
|---|---|
| უფასო ვებ-სერვისი 15 წუთში ითიშება | პირველი მოთხოვნა 30–60 წამს გრძელდება |
| უფასო PostgreSQL ვადა ეწურება ~30 დღეში | დემონსტრაციამდე ცოტა ხნით ადრე გააკეთეთ deploy |
| 512 MB RAM, 0.1 CPU | საკმარისია დემონსტრაციისთვის, არა წარმადობის ტესტისთვის |
| Static site არ ითიშება | Flutter web ყოველთვის ხელმისაწვდომია |

**დემონსტრაციამდე 5 წუთით ადრე გახსენით backend-ის URL**, რომ სერვისი
გაიღვიძოს — თორემ პირველი ჩატვირთვა უხერხულად დიდხანს გასტანს.

---

## 4. სარეზერვო გეგმა

თუ განთავსება ვერ მოხერხდა, ლოკალური გაშვება ყოველთვის მუშაობს და
სრულ, ცალკეულ მიკროსერვისულ არქიტექტურას აჩვენებს:

```bash
cd backend
docker compose up -d --build
curl http://localhost:8080/api/cities

cd ../mobile
flutter run -d chrome --dart-define=API_BASE_URL=http://localhost:8080
```
